/**
 * The commit this bundle was built from, replaced at build time by Vite's
 * `define`. Declared rather than read from import.meta.env because it is not a
 * VITE_ variable — it comes from the build environment, not from .env.
 */
declare const __BUILD_ID__: string;
